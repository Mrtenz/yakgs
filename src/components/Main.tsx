import * as React from 'react';
import Navigation from './Navigation';

const Main: React.StatelessComponent<{}> = () => (
  <div className="main">
    <p className="header">YAKGS</p>
    <Navigation />
  </div>
);

export default Main;
